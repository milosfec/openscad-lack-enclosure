$fn=100;

rail_width = 12;
rail_depth = 250;
rail_height = rail_width;
rail_distance = 105;

box_top_height = 14; // space above the rail needed to fit Trofast

mount_size = box_top_height;
mount_width = 30;

screw_size = 4;
screw_length = 20;
screw_offset = 10;
screw_wall_offset = 2; // Clearance to better fit screwdriver